/// Asset path constants for EverWith.
/// Reference these instead of bare string literals so that a rename in
/// pubspec.yaml only needs to be updated in one place.
abstract final class AppAssets {
  static const String _imagesBase = 'assets/images';

  static const String elderlyPerson = '$_imagesBase/elderlyperson.png';
}
